 usuarios = ["sembala", "guigo","zard","밤하늘", "" ]

for usuario in usuarios:
    if not usuario:
        continue
        
        for letra in usuario 
        if letra.isspace():
              print(f"ignorando (usuario), (contem espaço vazio)")  


    else:
    print(f"cadastrando usuario (usuario)")    